# Festag canvases

Interactive Cursor Canvas prototypes for Festag product design.

- `festag-master-auth-onboarding.canvas.tsx` — Master Auth + Tagro Intelligence Layer
  (identity → goal → optional clarify → sources → prepare). Visual source of truth
  for Anthropic ivory light mode (`#FAF9F5`) and the post-auth Tagro blueprint loop.

Open in Cursor beside chat. Implementation lands in `app/(client-auth)`, `app/onboarding`,
`app/preparing`, and `lib/tagro/workspace-blueprint.ts`.
