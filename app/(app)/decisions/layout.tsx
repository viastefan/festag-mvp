import DecisionsShell from '@/components/decisions/DecisionsShell'

export default function DecisionsLayout({ children }: { children: React.ReactNode }) {
  return <DecisionsShell>{children}</DecisionsShell>
}
