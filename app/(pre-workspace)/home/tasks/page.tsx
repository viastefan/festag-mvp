'use client'

import AppShellModuleEmpty from '@/components/app-shell/AppShellModuleEmpty'

export default function HomeTasksPage() {
  return (
    <AppShellModuleEmpty
      title="Tasks"
      description="Tasks appear once a workspace is ready. Create a workspace to begin."
    />
  )
}
