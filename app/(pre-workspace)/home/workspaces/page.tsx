'use client'

import AppShellModuleEmpty from '@/components/app-shell/AppShellModuleEmpty'

export default function HomeWorkspacesPage() {
  return (
    <AppShellModuleEmpty
      title="Workspaces"
      description="You don’t have a workspace yet. Create one to start building with Tagro."
      ctaLabel="Create Workspace"
    />
  )
}
