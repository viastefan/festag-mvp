/**
 * Festag popup surface — shared tokens for portaled panels (Cmd+K, workspace menu, …).
 * Canonical styles: `festag-popup-styles.css` (imported in globals.css).
 */

export const FESTAG_POPUP_RADIUS = '12px'

/** @deprecated Styles are global via festag-popup-styles.css — use class names only. */
export const FESTAG_POPUP_CSS = ''
